# Example production environment configuration
# Inherits from sync.conf and overrides for production.

[task]
name = "prod-sync"
target = "./deploy-prod/public"
mode = "mirror"
delete_orphans = true

[inherit]
file = "sync.conf"
