# Example dev environment configuration
# Inherits from sync.conf and overrides specific values.

[task]
name = "dev-sync"
target = "./deploy-dev/public"
exclude = ["*.map", "*.ts"]

[inherit]
file = "sync.conf"
